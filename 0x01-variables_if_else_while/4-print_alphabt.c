#include <stdio.h>
#include <stdlib.h>

/**
* main - print alphabets with omission using putchar 
*
* Return: Always 0.
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	ch != 'e';
	ch != 'q';

	putchar('\n');
	return (0);

}
